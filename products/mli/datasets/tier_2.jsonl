{"id": "t2-001", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement (no markdown fences, no commentary).\n\nQuestion: List every client name in the matters table.", "expected": {"rows": [["Acme Ltd"], ["Globex Inc"], ["Initech LLC"], ["Soylent Ltd"], ["Umbrella Co"]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "projection"]}
{"id": "t2-002", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: For each matter_id, return the matter_id and the count of documents.", "expected": {"rows": [[1, 2], [2, 2], [3, 1], [4, 2], [5, 3]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "groupby"]}
{"id": "t2-003", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the client column for every matter in the 'litigation' practice.", "expected": {"rows": [["Globex Inc"], ["Umbrella Co"]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "filter"]}
{"id": "t2-004", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return each document kind together with the total number of pages across all documents of that kind.", "expected": {"rows": [["contract", 35], ["evidence", 109], ["memo", 10], ["pleading", 50], ["policy", 12]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "aggregate"]}
{"id": "t2-005", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the distinct client name of every matter that has at least one document of kind 'contract'.", "expected": {"rows": [["Acme Ltd"], ["Initech LLC"]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "join"]}
{"id": "t2-006", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the matter_id of every matter that has more than two documents.", "expected": {"rows": [[5]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "having"]}
{"id": "t2-007", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return every client name, sorted alphabetically ascending.", "expected": {"rows": [["Acme Ltd"], ["Globex Inc"], ["Initech LLC"], ["Soylent Ltd"], ["Umbrella Co"]], "order_matters": true, "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "orderby"]}
{"id": "t2-008", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the matter_id of the single 'evidence' document with the most pages.", "expected": {"rows": [[2]], "order_matters": true, "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "limit"]}
{"id": "t2-009", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the total number of pages across all documents of kind 'memo'.", "expected": {"rows": [[10]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "scalar"]}
{"id": "t2-010", "input": "You are querying a SQLite database with tables matters(id, client, practice, opened_on) and documents(id, matter_id, kind, pages). Respond with ONLY a SQL SELECT statement.\n\nQuestion: Return the number of matters whose opened_on date is in March 2026 (YYYY-MM = 2026-03).", "expected": {"rows": [[2]], "schemas": {"run_sql": {"type": "object", "required": ["sql"], "properties": {"sql": {"type": "string"}}}}}, "tags": ["sql", "date"]}
