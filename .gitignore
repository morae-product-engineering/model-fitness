node_modules/
.next/
test-results/
playwright-report/
blob-report/
playwright/.cache/
testrail-run-url.txt

# Local seed DB built by scripts/seed_dev_runs.py and baseline-matrix.yml.
# The dev environment's source of truth is the seed blob in Azure Storage;
# the file under data/ is a working copy, not version-controlled state.
data/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
dist/
build/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.cover

# macOS
.DS_Store
**/.DS_Store
# Finder/iCloud copy-on-conflict duplicates (e.g. "foo 2.tsx").
* 2.*
* 3.*

# TypeScript incremental build cache
*.tsbuildinfo
# Structurizr Lite local cache
docs/architecture/.structurizr/
docs/architecture/workspace.json
# Defence-in-depth secret patterns
.env
.env.*
!.env.example
*.pem
*.key
*.p12
# VSCode personal overrides
.vscode/settings.local.json
# Claude Code session-generated settings (auto-written allow entries)
.claude/settings.json
