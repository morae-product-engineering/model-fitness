Adapter implementations that connect the platform to model providers. Each binding implements `BindingPlugin` from `mmfp/plugins/` and abstracts provider-specific details (authentication, request format, streaming) so the rest of the platform can invoke any model through a uniform interface. The first binding targets Azure AI Foundry.
