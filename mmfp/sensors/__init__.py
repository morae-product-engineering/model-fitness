"""Drift sensors — the SensorPlugin boundary and its concrete sensors.

The fourth P3 stable boundary (`SensorPlugin`) and the first concrete sensor
(`DriftSensor`) land in Slice 7 (MFP-93+). This package is created empty by the
Slice 7 acceptance test (MFP-92) so that `mmfp.sensors.tests` collects cleanly;
the sensor implementation itself does not exist yet.
"""
