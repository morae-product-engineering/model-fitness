Output sink implementations that publish evaluation results to external systems. Each reporter implements `ReporterPlugin` from `platform/plugins/` and handles a specific destination — LangSmith for agent quality traces, file output for offline archiving, console output for local development. Reporters are called after a `MatrixRun` completes.
