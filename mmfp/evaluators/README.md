Concrete evaluator implementations grouped by strategy: `deterministic/` for rule-based checks (regex, schema validation, cost calculation), `inferential/` for LLM-judge evaluators that call a model to score a response, and `composite/` for multi-step or aggregating evaluators that combine results from simpler ones. All evaluators implement `EvaluatorPlugin` from `mmfp/plugins/`.
