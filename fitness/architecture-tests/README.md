Tests that enforce architectural rules across the codebase (P4 — fitness functions). These are automated checks that verify structural constraints — for example, that `platform/core/` has no imports from `platform/api/`, or that no plugin implementation imports from another plugin's package. Failures here mean architecture drift, not a logic bug.
