Tests that enforce the P3 stable boundaries — the four plugin interfaces (`EvaluatorPlugin`, `BindingPlugin`, `ReporterPlugin`, `SensorPlugin`). These tests verify that every concrete implementation correctly satisfies the interface contract, and that no caller depends on implementation details that aren't part of the interface. Adding a new implementation must not require changes here.
